<!doctype html>
<html lang="it">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>Rinnovo servizio adesivo</title>
  <link rel="stylesheet" href="/style.css">
  <link rel="icon" href="/favicon.ico" sizes="any">
  <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32.png">
  <link rel="apple-touch-icon" sizes="180x180" href="/favicon-180.png">
  <link rel="icon" type="image/png" sizes="192x192" href="/favicon-192.png">
  <link rel="icon" type="image/png" sizes="512x512" href="/favicon-512.png">
  <style>
    :root{
      --accent:#2c74ff;
      --accent2:#5aa0ff;
      --bg1:#050b14;
      --bg2:#0a1320;
      --line:rgba(255,255,255,.10);
      --text:#f7fbff;
      --muted:#b6c2d4;
    }
    *{box-sizing:border-box}
    html,body{
      margin:0;padding:0;min-height:100%;
      font-family:-apple-system,BlinkMacSystemFont,"SF Pro Display","Segoe UI",Arial,sans-serif;
      background:
        radial-gradient(circle at 18% 0%, rgba(33,78,166,.22), transparent 28%),
        linear-gradient(180deg, var(--bg1) 0%, #07101b 48%, var(--bg2) 100%);
      color:var(--text)
    }
    .wrap{
      width:min(900px, calc(100% - 24px));
      margin:0 auto;
      padding:32px 0 40px;
    }
    .panel{
      border-radius:30px;
      overflow:hidden;
      background:
        radial-gradient(circle at 70% 8%, rgba(45,110,240,.12), transparent 24%),
        linear-gradient(180deg, rgba(11,21,35,.99) 0%, rgba(8,16,27,.995) 100%);
      border:1px solid rgba(255,255,255,.08);
      box-shadow:0 24px 56px rgba(0,0,0,.38);
      padding:30px 26px 28px;
    }
    .badge{
      display:inline-block;
      padding:8px 12px;
      border-radius:999px;
      background:rgba(255,255,255,.05);
      border:1px solid var(--line);
      color:#dce7f7;
      font-size:.88rem;
      margin-bottom:16px;
    }
    h1{
      margin:0;
      line-height:.95;
      letter-spacing:-.06em;
      font-weight:900;
      font-size:clamp(2.2rem, 5.2vw, 3.8rem);
      text-transform:uppercase;
      color:#fff;
    }
    h1 span{
      display:block;
      margin-top:6px;
      color:var(--accent2);
      text-shadow:0 0 18px rgba(90,160,255,.25);
    }
    .line{
      width:62px;height:5px;border-radius:999px;
      background:linear-gradient(90deg,var(--accent),var(--accent2));
      margin:18px 0 14px;
      box-shadow:0 0 18px rgba(90,160,255,.30);
    }
    .sub{
      margin:0;
      color:var(--muted);
      font-size:1rem;
      line-height:1.6;
      max-width:720px;
    }
    .grid{
      display:grid;
      grid-template-columns:repeat(3, 1fr);
      gap:16px;
      margin-top:26px;
    }
    .card{
      border-radius:22px;
      background:linear-gradient(180deg, rgba(255,255,255,.03) 0%, rgba(255,255,255,.015) 100%);
      border:1px solid var(--line);
      padding:20px;
    }
    .card h2{
      margin:0 0 10px;
      color:#fff;
      font-size:1.08rem;
      font-weight:900;
      text-transform:uppercase;
    }
    .card p{
      margin:0;
      color:#c2cfdf;
      line-height:1.55;
      font-size:.95rem;
    }
    .actions{
      display:flex;
      gap:12px;
      flex-wrap:wrap;
      margin-top:24px;
    }
    .btn{
      display:inline-flex;
      align-items:center;
      justify-content:center;
      min-height:54px;
      padding:0 20px;
      border-radius:16px;
      text-decoration:none;
      color:#fff;
      font-weight:900;
      font-size:.98rem;
    }
    .btn-primary{
      background:linear-gradient(135deg,var(--accent) 0%,var(--accent2) 100%);
      box-shadow:0 16px 30px rgba(18,85,204,.32);
    }
    .btn-secondary{
      background:rgba(255,255,255,.05);
      border:1px solid var(--line);
    }
    .note{
      margin-top:22px;
      border-radius:18px;
      padding:15px 18px;
      background:rgba(255,255,255,.022);
      border:1px solid var(--line);
      color:#c1ccdb;
      line-height:1.56;
      font-size:.94rem;
    }
    .note strong{color:#fff}
    @media (max-width:760px){
      .grid{grid-template-columns:1fr}
      .wrap{width:min(900px, calc(100% - 16px))}
      .panel{padding:22px 16px 20px}
    }
  </style>
</head>
<body>
  <div class="wrap">
    <div class="panel">
      <div class="badge">Servizio non attivo</div>
      <h1>RINNOVA<span>IL SERVIZIO</span></h1>
      <div class="line"></div>
      <p class="sub">
        Questo adesivo non è attualmente operativo. Per continuare a ricevere segnalazioni e mantenere attiva la pagina di contatto,
        è necessario rinnovare il servizio oppure acquistare un nuovo piano.
      </p>

      <div class="grid">
        <div class="card">
          <h2>Rinnovo 1 mese</h2>
          <p>Ideale per riattivare rapidamente il servizio e mantenere operativo il QR per un periodo breve.</p>
        </div>
        <div class="card">
          <h2>Rinnovo 6 mesi</h2>
          <p>La soluzione più equilibrata per mantenere il servizio attivo a medio termine senza pensieri.</p>
        </div>
        <div class="card">
          <h2>Sempre valido</h2>
          <p>Versione definitiva, pensata per chi desidera lasciare il servizio sempre attivo senza rinnovi periodici.</p>
        </div>
      </div>

      <div class="actions">
        <a href="#" class="btn btn-primary">Rinnova il servizio</a>
        <a href="#" class="btn btn-secondary">Acquista una nuova attivazione</a>
      </div>

      <div class="note">
        <strong>Nota:</strong> questa è una pagina predisposta per il rinnovo. La funzione di pagamento verrà collegata successivamente.
      </div>
    </div>
  </div>
</body>
</html>
