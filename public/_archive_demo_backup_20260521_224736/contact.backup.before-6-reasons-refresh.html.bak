<!DOCTYPE html>
<html lang="it">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>Contatto veicolo</title>
  <style>
    :root{
      --bg:#eef3f8;
      --card:#ffffff;
      --text:#172033;
      --muted:#5f6b7b;
      --line:#dde5ef;
      --primary:#2f6fed;
      --primary-soft:#eaf1ff;
      --shadow:0 18px 48px rgba(20,34,60,.08);
      --radius:24px;
    }
    *{box-sizing:border-box}
    html,body{margin:0;padding:0;font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",Roboto,Helvetica,Arial,sans-serif;background:linear-gradient(180deg,#f6f9fc 0%, #edf2f7 100%);color:var(--text)}
    body{min-height:100vh}
    .page{min-height:100vh;display:flex;align-items:flex-start;justify-content:center;padding:20px 14px 34px}
    .shell{width:100%;max-width:760px}
    .top-card,.form-card{background:var(--card);border:1px solid rgba(23,32,51,.06);border-radius:var(--radius);box-shadow:var(--shadow)}
    .top-card{padding:22px 20px;margin-bottom:14px}
    .vehicle-kicker{display:inline-flex;align-items:center;padding:7px 12px;border-radius:999px;background:var(--primary-soft);color:var(--primary);font-size:.84rem;font-weight:800;letter-spacing:.04em;text-transform:uppercase}
    .vehicle-head{display:flex;align-items:center;justify-content:space-between;gap:16px;flex-wrap:wrap;margin-top:16px}
    .vehicle-head-main{min-width:240px;flex:1}
    .vehicle-name{margin:0;font-size:1.72rem;line-height:1.03;letter-spacing:-.05em;font-weight:900;color:var(--text)}
    .vehicle-subline{margin-top:8px;color:var(--muted);font-size:.95rem;line-height:1.45}
    .plate{display:inline-flex;align-items:center;justify-content:center;min-width:160px;padding:12px 16px;border-radius:16px;background:#f3f6fa;border:1px solid var(--line);font-weight:900;letter-spacing:.08em;color:#21304a;font-size:1.18rem}
    .hero-title{margin:18px 0 0;font-size:1.38rem;line-height:1.12;font-weight:900;letter-spacing:-.03em;color:var(--text)}
    .hero-sub{margin:10px 0 0;color:var(--muted);font-size:1rem;line-height:1.58}
    .hero-note{margin-top:12px;color:#425167;font-size:.95rem;line-height:1.5;padding:12px 14px;border-radius:16px;background:#f7f9fc;border:1px solid var(--line)}
    .form-card{padding:20px}
    .section-title{margin:0 0 6px;font-size:1.1rem;font-weight:900;letter-spacing:-.02em;color:var(--text)}
    .section-sub{margin:0 0 16px;color:var(--muted);font-size:.96rem;line-height:1.5}
    .reason-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-bottom:18px}
    .reason-card{border:1px solid var(--line);background:#fff;border-radius:18px;padding:14px 15px;cursor:pointer;transition:.18s ease;display:flex;align-items:flex-start;gap:12px}
    .reason-card:hover{border-color:#cfdcf6;box-shadow:0 10px 24px rgba(47,111,237,.07);transform:translateY(-1px)}
    .reason-card.active{border-color:rgba(47,111,237,.45);background:var(--primary-soft);box-shadow:0 10px 24px rgba(47,111,237,.10)}
    .reason-dot{display:none}
    .reason-card.active .reason-dot{border-color:var(--primary)}
    .reason-card.active .reason-dot::after{content:"";position:absolute;inset:3px;border-radius:999px;background:var(--primary)}
    .reason-main{font-size:.98rem;font-weight:800;color:var(--text);line-height:1.2}
    .reason-help{display:none}
    .field-block{margin-top:14px}
    .label{display:block;margin:0 0 8px;font-size:.95rem;font-weight:800;color:var(--text)}
    .label small{font-weight:600;color:var(--muted)}
    .hint{margin:7px 0 0;color:var(--muted);font-size:.88rem;line-height:1.45}
    .field,.textarea{width:100%;border:1px solid var(--line);background:#fff;border-radius:16px;padding:15px 15px;font-size:1rem;color:var(--text);outline:none;transition:border-color .15s ease, box-shadow .15s ease}
    .field:focus,.textarea:focus{border-color:rgba(47,111,237,.48);box-shadow:0 0 0 4px rgba(47,111,237,.10)}
    .textarea{min-height:118px;resize:vertical;line-height:1.5}
    .count{margin-top:6px;text-align:right;font-size:.82rem;color:var(--muted)}
    .check{display:flex;align-items:flex-start;gap:10px;margin-top:16px;padding:14px 14px;border-radius:16px;background:#f7f9fc;border:1px solid var(--line);color:#405066;font-size:.94rem;line-height:1.45}
    .check input{margin-top:2px}
    .primary-btn{width:100%;min-height:58px;margin-top:18px;border:0;border-radius:18px;background:linear-gradient(135deg,#2a67e6 0%,#4b8cff 100%);color:#fff;font-size:1rem;font-weight:900;letter-spacing:.01em;cursor:pointer;box-shadow:0 14px 28px rgba(47,111,237,.24)}
    .note{margin-top:16px;padding:14px 14px;border-radius:16px;background:#fbfcfe;border:1px solid var(--line);color:var(--muted);font-size:.9rem;line-height:1.5}
    .footer-note{margin-top:14px;text-align:center;color:#465469;font-size:.92rem;line-height:1.5}
    .loading,.error{text-align:center;padding:24px;border-radius:20px;background:#fff;border:1px solid var(--line);box-shadow:var(--shadow)}
    .error{color:#9e352d;background:#fff3f1;border-color:#f2d0cb}
    .confirm-modal{position:fixed;inset:0;display:none;align-items:center;justify-content:center;padding:18px;background:rgba(11,18,31,.45);backdrop-filter:blur(4px);z-index:9999}
    .confirm-card{width:min(520px,100%);background:#fff;border-radius:24px;border:1px solid rgba(23,32,51,.08);box-shadow:0 24px 60px rgba(20,34,60,.16);padding:22px}
    .confirm-title{margin:0;font-size:1.2rem;font-weight:900;color:#1d6a3a}
    .confirm-text{margin-top:10px;color:var(--text);line-height:1.55;font-size:.98rem}
    .confirm-actions{margin-top:18px;display:flex;gap:10px;flex-wrap:wrap}
    .secondary-btn{min-height:48px;padding:0 16px;border:1px solid var(--line);border-radius:14px;background:#fff;color:var(--text);font-weight:800;cursor:pointer}
    @media (max-width:719px){
      .vehicle-head{flex-direction:column;align-items:flex-start}
      .vehicle-name{font-size:1.85rem}
      .plate{min-width:auto}
      .top-card,.form-card{padding:22px 18px}
    }
    @media (min-width:720px){
      .page{padding:28px 18px 40px}
      .top-card,.form-card{padding:24px}
      .reason-grid{grid-template-columns:1fr 1fr}
    }
  </style>
</head>
<body>
  <div class="page">
    <div class="shell">
      <div id="contactLoading" class="loading">Caricamento in corso…</div>
      <div id="contactError" class="error" style="display:none;"></div>

      <div id="contactContent" style="display:none;">
        <div class="top-card">
          <div class="vehicle-kicker">Segnalazione veicolo</div>

          <div class="vehicle-head">
            <div class="vehicle-head-main">
              <div class="vehicle-name" id="vehicleName">Veicolo</div>
              <div class="vehicle-subline">Stai inviando una segnalazione per questo veicolo</div>
            </div>
            <div class="plate" id="vehiclePlate">---</div>
          </div>

          <h1 class="hero-title">Hai un problema con questa auto?</h1>
          <p class="hero-sub">
            Seleziona il motivo. Il proprietario verrà avvisato subito.
          </p>
        </div>

        <div class="form-card">
          <h2 class="section-title">Motivo della segnalazione</h2>
          <p class="section-sub">
            Tocca una voce e invia. La posizione resta facoltativa.
          </p>

          <div class="reason-grid" id="reasonGrid">
            <button type="button" class="reason-card" data-reason="Urtata accidentalmente"><span class="reason-main">Urtata accidentalmente</span></button>
            <button type="button" class="reason-card" data-reason="Intralcio al traffico"><span class="reason-main">Intralcio al traffico</span></button>
            <button type="button" class="reason-card" data-reason="Vetri abbassati"><span class="reason-main">Vetri abbassati</span></button>
            <button type="button" class="reason-card" data-reason="Porta aperta"><span class="reason-main">Porta aperta</span></button>
            <button type="button" class="reason-card" data-reason="Bimbo o animale in auto"><span class="reason-main">Bimbo o animale in auto</span></button>
            <button type="button" class="reason-card" data-reason="Furto"><span class="reason-main">Furto</span></button>
            <button type="button" class="reason-card" data-reason="Tentato furto"><span class="reason-main">Tentato furto</span></button>
            <button type="button" class="reason-card" data-reason="Varie"><span class="reason-main">Varie</span></button>
          </div>

          <input type="hidden" id="reason">

          <div class="field-block">
            <label class="label" for="msg">Dettagli aggiuntivi <small>(facoltativo)</small></label>
            <textarea id="msg" class="textarea" maxlength="500" placeholder="Scrivi qui, solo se necessario..."></textarea>
            <div class="count" id="charCount">0/500</div>
          </div>

          <div class="check">
            <input type="checkbox" id="shareLocation">
            <label for="shareLocation">Condividi anche la mia posizione attuale, se può aiutare il proprietario a capire meglio la situazione. <strong>(facoltativo)</strong></label>
          </div>

          <div class="field-block">
            <label class="label" for="senderPhone">Tuo nome o recapito <small>(facoltativo)</small></label>
            <input id="senderPhone" class="field" type="text" inputmode="text" maxlength="60" placeholder="Es. Marco oppure 3331234567">
          </div>

          <button class="primary-btn" id="sendBtn" type="button">INVIA SEGNALAZIONE ORA</button>

          <div class="privacy">Notifica riservata. Nessun numero visibile.</div>

          <div class="final-note">
            <strong>Usa questo servizio solo per segnalazioni reali.</strong><br>
            In caso di emergenza grave contatta subito i numeri di soccorso.
          </div>

          <div id="stickerPreview" class="sticker-preview" style="display:none;">
            <div class="sticker-preview-label">Adesivo identificativo del servizio presente sul veicolo</div>
            <a id="stickerPreviewLink" class="service-cta-link" href="#" target="_blank" rel="noopener noreferrer">Apri anteprima adesivo</a>
          </div>

          <div class="service-cta" style="margin-top:24px;padding-top:20px;border-top:1px solid rgba(20,32,51,.06);text-align:center;">
            <div class="service-cta-title" style="display:block;max-width:420px;margin:0 auto;line-height:1.55;text-align:center;">Vuoi essere avvisato subito anche per la tua vettura?</div>
            <div style="display:block;margin-top:10px;text-align:center;">
              <a class="service-cta-link" href="https://adesivo-auto.onrender.com/scopri-servizio" style="display:inline-block;text-align:center;">Scopri il servizio</a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <div id="internalConfirmModal" class="confirm-modal">
    <div class="confirm-card">
      <h3 class="confirm-title">Notifica inviata al proprietario.</h3>
      <div class="confirm-text">Il sistema ha registrato correttamente la segnalazione.</div>
      <div class="confirm-actions"><button id="confirmCloseBtn" type="button" class="secondary-btn">Chiudi</button></div>
    </div>
  </div>

  <script>
    const params = new URLSearchParams(window.location.search);
    const pathMatch = window.location.pathname.match(/\/contact\/u\/([A-Z0-9]+)/i);
    const publicId = ((params.get('public_id') || (pathMatch ? pathMatch[1] : ''))).trim().toUpperCase();

    const loadingEl = document.getElementById('contactLoading');
    const errorEl = document.getElementById('contactError');
    const contentEl = document.getElementById('contactContent');

    let contactData = null;

    const msgField = document.getElementById('msg');
    const charCount = document.getElementById('charCount');
    const reasonField = document.getElementById('reason');
    const sendBtn = document.getElementById('sendBtn');
    const senderPhoneField = document.getElementById('senderPhone');
    const internalConfirmModal = document.getElementById('internalConfirmModal');
    const confirmCloseBtn = document.getElementById('confirmCloseBtn');
    const reasonCards = Array.from(document.querySelectorAll('.reason-card'));

    const presetMessages = {
      'Urtata accidentalmente': 'Segnalazione di possibile urto al veicolo.',
      'Intralcio al traffico': 'Il veicolo si trova in una situazione di intralcio al traffico.',
      'Vetri abbassati': 'I vetri del veicolo risultano abbassati.',
      'Porta aperta': 'Una porta del veicolo risulta aperta.',
      'Bimbo o animale in auto': 'Presenza di bimbo o animale in auto.',
      'Furto': 'Possibile furto in corso relativo al veicolo.',
      'Tentato furto': 'Possibile tentativo di furto relativo al veicolo.',
      'Varie': ''
    };

    reasonCards.forEach(card => {
      card.addEventListener('click', () => {
        reasonCards.forEach(c => c.classList.remove('active'));
        card.classList.add('active');
        const reason = card.dataset.reason || '';
        reasonField.value = reason;
        if (!msgField.value.trim() && presetMessages[reason]) {
          msgField.value = presetMessages[reason];
          updateCharCount();
        }
      });
    });

    function updateCharCount() {
      charCount.textContent = `${msgField.value.length}/500`;
    }
    msgField.addEventListener('input', updateCharCount);

    function showError(message) {
      loadingEl.style.display = 'none';
      contentEl.style.display = 'none';
      errorEl.style.display = 'block';
      errorEl.textContent = message;
    }

    function applyVehicleTheme(color) {
      const c = (color || '').toLowerCase();
      if (!c) return;
      if (c.includes('rosso')) document.documentElement.style.setProperty('--primary', '#cf4534');
      if (c.includes('blu')) document.documentElement.style.setProperty('--primary', '#2f6fed');
      if (c.includes('nero')) document.documentElement.style.setProperty('--primary', '#3b4658');
      if (c.includes('bianco')) document.documentElement.style.setProperty('--primary', '#5e7aa6');
      if (c.includes('grigio')) document.documentElement.style.setProperty('--primary', '#536170');
    }

    function renderContactData(data) {
      contactData = data;
      const vehicleName = [data.brand, data.vehicle_model].filter(Boolean).join(' ').trim() || data.vehicle_model || data.brand || 'Veicolo';
      document.getElementById('vehicleName').innerText = vehicleName;
      document.getElementById('vehiclePlate').innerText = data.plate || 'Targa non disponibile';
      applyVehicleTheme(data.color || '');
      loadingEl.style.display = 'none';
      errorEl.style.display = 'none';
      contentEl.style.display = 'block';
      updateCharCount();
      const previewWrap = document.getElementById('stickerPreview');
      const previewLink = document.getElementById('stickerPreviewLink');
      if (previewWrap && previewLink && data.code) {
        previewLink.href = `/sticker.html?code=${encodeURIComponent(data.code)}`;
        previewLink.textContent = 'Apri anteprima adesivo';
        previewWrap.style.display = 'block';
      }


    }

    function buildMessage(extraLocation = '') {
      const vehicleName = [contactData.brand, contactData.vehicle_model].filter(Boolean).join(' ').trim() || 'Veicolo';
      const reason = reasonField.value;
      const msg = msgField.value.trim();
      const senderPhone = (senderPhoneField?.value || '').trim();
      let text = `Segnalazione per ${vehicleName} (${contactData.plate || ''})\n`;
      if (reason) text += `Motivo: ${reason}\n`;
      if (msg) text += `Dettagli: ${msg}\n`;
      if (senderPhone) text += `Numero per eventuale richiamo: ${senderPhone}\n`;
      if (extraLocation) text += extraLocation;
      return text.trim();
    }

    async function logInternalMessage(finalMessage, locationData = {}) {
      try {
        await fetch('/api/log-contact-message', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            code: contactData.code,
            plate: contactData.plate,
            brand: contactData.brand,
            vehicle_model: contactData.vehicle_model,
            color: contactData.color,
            reason: reasonField.value,
            message_text: finalMessage,
            sender_phone: (senderPhoneField?.value || '').trim() || null,
            location_shared: !!locationData.location_shared,
            latitude: locationData.latitude || null,
            longitude: locationData.longitude || null,
            maps_url: locationData.maps_url || null
          })
        });
      } catch (e) {}
    }

    async function goInternal() {
      const shareLocation = document.getElementById('shareLocation')?.checked;
      const baseMessage = buildMessage();

      if (!reasonField.value) {
        alert('Seleziona un motivo prima di inviare la segnalazione.');
        return;
      }

      if (!shareLocation) {
        await logInternalMessage(baseMessage);
        internalConfirmModal.style.display = 'flex';
        return;
      }

      if (!navigator.geolocation) {
        await logInternalMessage(baseMessage);
        internalConfirmModal.style.display = 'flex';
        return;
      }

      navigator.geolocation.getCurrentPosition(
        async (position) => {
          const lat = position.coords.latitude.toFixed(6);
          const lng = position.coords.longitude.toFixed(6);
          const mapsUrl = `https://maps.google.com/?q=${lat},${lng}`;
          const extraLocation = `Posizione: ${lat}, ${lng}\nMappa: ${mapsUrl}\n`;
          const finalMessage = buildMessage(extraLocation);

          await logInternalMessage(finalMessage, {
            location_shared: true,
            latitude: lat,
            longitude: lng,
            maps_url: mapsUrl
          });

          internalConfirmModal.style.display = 'flex';
        },
        async () => {
          await logInternalMessage(baseMessage);
          internalConfirmModal.style.display = 'flex';
        },
        { enableHighAccuracy: true, timeout: 10000, maximumAge: 0 }
      );
    }

    sendBtn.addEventListener('click', goInternal);
    confirmCloseBtn.addEventListener('click', () => { internalConfirmModal.style.display = 'none'; });
    internalConfirmModal.addEventListener('click', (e) => { if (e.target === internalConfirmModal) internalConfirmModal.style.display = 'none'; });

    async function init() {
      if (!publicId) {
        showError('Identificativo pubblico mancante.');
        return;
      }
      try {
        const response = await fetch(`/api/public-contact/${encodeURIComponent(publicId)}`);
        const payload = await response.json();
        if (!response.ok || !payload.success) {
          showError(payload.error || 'Veicolo non trovato o non attivo.');
          return;
        }
        renderContactData(payload.data);
      } catch (err) {
        showError('Errore di caricamento della pagina.');
      }
    }

    init();
  </script>
</body>
</html>
