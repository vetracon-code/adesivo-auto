<!DOCTYPE html>
<html lang="it">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>Contatto veicolo</title>
  <style>
    :root{
      --bg:#eef3fb;
      --card:#ffffff;
      --text:#101828;
      --muted:#667085;
      --line:#e7ecf3;
      --blue:#0a84ff;
      --blue-dark:#0066d6;
      --soft:#f8fbff;
      --success-bg:#ecfdf3;
      --success-bd:#b7ebc6;
      --success-tx:#166534;
      --danger:#b42318;
    }
    *{box-sizing:border-box}
    html,body{margin:0;padding:0}
    body{
      font-family:-apple-system,BlinkMacSystemFont,"SF Pro Display","Segoe UI",Roboto,Helvetica,Arial,sans-serif;
      background:linear-gradient(180deg,#edf4ff 0%, #f7f9fc 100%);
      color:var(--text);
      min-height:100vh;
    }
    .wrap{
      max-width:720px;
      margin:0 auto;
      padding:16px 14px 40px;
    }
    .card{
      background:var(--card);
      border:1px solid rgba(255,255,255,.92);
      border-radius:26px;
      box-shadow:0 16px 40px rgba(16,24,40,.08);
      overflow:hidden;
    }
    .hero{
      padding:20px 20px 18px;
      text-align:center;
      border-bottom:1px solid var(--line);
      background:linear-gradient(180deg,#ffffff 0%, #f9fbff 100%);
    }
    .eyebrow{
      font-size:12px;
      font-weight:800;
      letter-spacing:.10em;
      text-transform:uppercase;
      color:var(--blue);
      margin-bottom:8px;
    }
    h1{
      margin:0;
      font-size:34px;
      line-height:1.02;
      letter-spacing:-.045em;
      font-weight:850;
    }
    .sub{
      margin:10px auto 0;
      max-width:430px;
      font-size:16px;
      line-height:1.38;
      color:var(--muted);
    }
    .privacy-hero{
      margin:12px auto 0;
      max-width:470px;
      font-size:14px;
      line-height:1.45;
      color:#475467;
    }
    .privacy-hero strong{
      color:#1d2939;
    }
.vehicle{
      margin:16px 20px 0;
      padding:14px 16px;
      border:1px solid var(--line);
      border-radius:18px;
      background:#fafcff;
      display:flex;
      align-items:center;
      justify-content:center;
      gap:14px;
      flex-wrap:wrap;
    }
    .vehicle-mark{
      display:none;
    }
    .vehicle-text{
      text-align:center;
    }
    .vehicle-name{
      font-size:20px;
      font-weight:800;
      letter-spacing:-.025em;
      line-height:1.12;
    }
    .plate{
      font-size:14px;
      color:var(--muted);
      margin-top:4px;
      font-weight:700;
      letter-spacing:.03em;
    }
    .body{
      padding:18px 18px 24px;
    }
    .section-title{
      font-size:14px;
      font-weight:800;
      margin:0 0 10px;
      color:#344054;
    }
    .choices{
      display:grid;
      grid-template-columns:1fr 1fr;
      gap:10px;
    }
    .choice{
      position:relative;
      display:flex;
      align-items:center;
      justify-content:center;
      min-height:66px;
      padding:14px 14px;
      border:1px solid var(--line);
      border-radius:18px;
      background:var(--soft);
      font-size:15px;
      font-weight:750;
      line-height:1.2;
      text-align:center;
      color:#17212f;
      cursor:pointer;
      user-select:none;
      transition:.16s ease;
    }
    .choice:hover{
      transform:translateY(-1px);
      border-color:#cdd9ea;
    }
    .choice.selected{
      background:linear-gradient(180deg,#ebf5ff 0%, #dcebff 100%);
      border-color:rgba(10,132,255,.45);
      box-shadow:0 0 0 4px rgba(10,132,255,.10);
      color:#0b3b79;
    }
    .field{
      margin-top:16px;
    }
    label{
      display:block;
      margin:0 0 8px;
      font-size:14px;
      font-weight:800;
      color:#344054;
    }
    textarea,input{
      width:100%;
      border:1px solid var(--line);
      border-radius:18px;
      background:#fff;
      padding:15px 16px;
      font-size:16px;
      outline:none;
      transition:.16s ease;
    }
    textarea{
      min-height:60px;
      height:60px;
      padding:10px 12px;
      resize:vertical;
      line-height:1.35;
    }
    textarea:focus,input:focus{
      border-color:rgba(10,132,255,.45);
      box-shadow:0 0 0 4px rgba(10,132,255,.10);
    }
    .micro{
      margin-top:8px;
      font-size:13px;
      line-height:1.38;
      color:var(--muted);
    }
    .share-box{
      margin-top:18px;
      padding:14px 14px;
      border:1px solid var(--line);
      border-radius:18px;
      background:#fbfdff;
      display:flex;
      align-items:flex-start;
      gap:10px;
    }
    .share-box input{
      width:auto;
      margin-top:2px;
      box-shadow:none;
    }
    .share-box label{
      margin:0;
      font-size:14px;
      line-height:1.42;
      font-weight:700;
      color:#344054;
    }
    .cta{
      margin-top:26px;
      width:100%;
      border:none;
      border-radius:20px;
      background:linear-gradient(180deg,var(--blue) 0%, var(--blue-dark) 100%);
      color:#fff;
      padding:19px 18px;
      font-size:17px;
      font-weight:850;
      letter-spacing:.01em;
      cursor:pointer;
      box-shadow:0 10px 24px rgba(10,132,255,.25);
      transition:transform .12s ease, opacity .12s ease;
    }
    .cta:active{transform:scale(.99)}
    .privacy{
      text-align:center;
      margin-top:16px;
      font-size:13px;
      color:var(--muted);
      line-height:1.45;
    }
    .msg{
      display:none;
      margin-top:16px;
      padding:14px 16px;
      border-radius:16px;
      font-size:14px;
      line-height:1.45;
    }
    .msg.error{
      display:none;
      margin-top:12px;
      padding:12px 14px;
      border-radius:14px;
      border:1px solid rgba(210, 63, 63, .16);
      background:rgba(210, 63, 63, .08);
      color:#a62d2d;
      font-size:.93rem;
      line-height:1.45;
      font-weight:700;
    }
    .msg.success{
      background:var(--success-bg);
      border:1px solid var(--success-bd);
      color:var(--success-tx);
    }
    .final-note{
      margin-top:18px;
      text-align:center;
      font-size:12px;
      line-height:1.48;
      color:var(--muted);
    }
    .final-note strong{color:var(--danger)}
    .service-cta{
      margin-top:24px;
      padding-top:22px;
      border-top:1px solid rgba(20,32,51,.06);
      text-align:center;
      display:flex;
      flex-direction:column;
      align-items:center;
      justify-content:center;
      gap:10px;
    }
    .service-cta-title{
      display:block;
      max-width:420px;
      margin:0 auto;
      line-height:1.55;
      text-align:center;
      font-size:15px;
      font-weight:800;
      color:#344054;
    }
    .service-cta-link{
      display:inline-block;
      text-align:center;
      color:var(--blue);
      font-weight:800;
      text-decoration:none;
    }
    .service-cta-link:hover{
      text-decoration:underline;
    }
    .choices.flash-attention .choice{
      animation:choiceAttentionPulse .75s ease 2;
    }
    @keyframes choiceAttentionPulse{
      0%{
        transform:scale(1);
        box-shadow:0 0 0 rgba(63,121,216,0);
        border-color:var(--line);
      }
      35%{
        transform:scale(1.02);
        box-shadow:0 0 0 4px rgba(63,121,216,.10);
        border-color:rgba(63,121,216,.40);
      }
      100%{
        transform:scale(1);
        box-shadow:0 0 0 rgba(63,121,216,0);
        border-color:var(--line);
      }
    }

    @media (max-width:560px){
      h1{font-size:30px}
      .choices{grid-template-columns:1fr 1fr; gap:8px}
      .choice{min-height:58px; padding:12px 10px; font-size:14px; line-height:1.25}
      .vehicle-name{font-size:18px}
    }
  
    .preview-topbar{
      display:none;
      position:sticky;
      top:0;
      z-index:50;
      padding:10px 14px;
      background:rgba(255,255,255,.92);
      backdrop-filter:blur(8px);
      border-bottom:1px solid var(--line);
    }
</style>
</head>

<body>
  <div class="wrap">
<div class="card">
      <div class="hero">
        <div class="eyebrow">Contatto veicolo</div>
        <h1>Vuoi avvisare il proprietario?</h1>

        <div class="vehicle">
          <div class="vehicle-text" style="width:100%;text-align:center;">
            <div class="vehicle-name" id="vehicleName">Veicolo</div>
            <div class="plate" id="vehiclePlate">---</div>
          </div>
        </div>

        <div class="privacy-hero"><strong style="font-size:1.08rem;letter-spacing:.04em;text-transform:uppercase;">NOTIFICA DI CORTESIA</strong><br><strong>Avvisa</strong> il proprietario <strong>senza mostrare il tuo numero</strong>.<br>Scegli un’opzione e, se utile, aggiungi la posizione del veicolo.</div>

      </div>

      <div class="body">
        <div class="choices" id="choices">
          <button type="button" class="choice" data-value="Urtata accidentalmente">Urtata accidentalmente</button>
          <button type="button" class="choice" data-value="Intralcio al traffico">Intralcio al traffico</button>
          <button type="button" class="choice" data-value="Vetri abbassati / porta aperta">Vetri abbassati / porta aperta</button>
          <button type="button" class="choice" data-value="Bimbo o animale in auto">Bimbo o animale in auto</button>
          <button type="button" class="choice" data-value="Furto / tentato furto">Furto / tentato furto</button>
          <button type="button" class="choice" data-value="Varie">Varie</button>
        </div>

        <input type="hidden" id="reason" />

        <div class="msg error" id="errorBox">Seleziona il motivo della segnalazione per poter avvisare il proprietario.</div>
        <div class="msg success" id="successBox">Notifica inviata al proprietario.</div>

        <div class="field">
          <label for="msg">Dettagli aggiuntivi <span style="font-weight:600;color:#667085;">(facoltativi)</span></label>
          <textarea id="msg" rows="1" maxlength="1500" placeholder="Scrivi qui, solo se necessario..."></textarea>
          <div class="micro">Puoi aggiungere un testo libero.</div>
        </div>

        <button class="cta" id="sendBtn" style="margin-bottom:14px;">INVIA SEGNALAZIONE</button>
        <div class="micro" style="margin-top:10px;text-align:center;font-size:.92rem;line-height:1.5;">
          <strong>Il proprietario verrà avvisato subito.</strong><br>
          La segnalazione è riservata ed anonima.
        </div>

        <div class="location-map-card" style="margin-top:18px;position:relative;overflow:hidden;border-radius:26px;border:1px solid rgba(63,121,216,.18);background:
          radial-gradient(circle at 18% 28%, rgba(63,121,216,.16) 0 2px, transparent 3px),
          radial-gradient(circle at 68% 38%, rgba(63,121,216,.13) 0 2px, transparent 3px),
          radial-gradient(circle at 42% 74%, rgba(63,121,216,.12) 0 2px, transparent 3px),
          linear-gradient(135deg, rgba(63,121,216,.10) 0%, rgba(255,255,255,.94) 45%, rgba(232,239,250,.92) 100%);
          box-shadow:0 14px 34px rgba(30,46,82,.08);padding:18px;">
          
          <div style="position:absolute;inset:0;opacity:.24;background-image:
            linear-gradient(90deg, rgba(63,121,216,.22) 1px, transparent 1px),
            linear-gradient(0deg, rgba(63,121,216,.18) 1px, transparent 1px);
            background-size:22px 22px;pointer-events:none;"></div>

          <div style="position:relative;z-index:1;display:flex;gap:14px;align-items:flex-start;">
            <div style="width:48px;height:48px;border-radius:18px;background:linear-gradient(180deg,#3f79d8,#2f5fad);display:flex;align-items:center;justify-content:center;color:#fff;font-size:1.45rem;box-shadow:0 10px 22px rgba(63,121,216,.24);flex:0 0 auto;">📍</div>

            <div style="flex:1;min-width:0;">
              <div style="font-weight:900;color:#223046;font-size:1.02rem;line-height:1.25;">Aggiungi la posizione</div>
              <div style="margin-top:5px;color:#5b6980;font-size:.93rem;line-height:1.48;">
                Facoltativo, ma molto utile: aiuta il proprietario a capire subito dove si trova il mezzo.
              </div>

              <label for="shareLocation" style="margin-top:14px;display:flex;align-items:center;gap:10px;width:100%;border:none;border-radius:18px;background:linear-gradient(180deg,#3f79d8 0%, #2f5fad 100%);color:#fff;font-weight:900;font-size:.96rem;min-height:52px;padding:0 16px;box-shadow:0 12px 26px rgba(63,121,216,.22);cursor:pointer;box-sizing:border-box;">
                <input type="checkbox" id="shareLocation" style="width:20px;height:20px;accent-color:#ffffff;flex:0 0 auto;" />
                <span>Condividi posizione attuale</span>
              </label>

              <div style="margin-top:9px;color:#667085;font-size:.82rem;line-height:1.42;">
                La posizione viene condivisa solo se attivi questo pulsante. Il tuo numero non viene mostrato.
              </div>
            </div>
          </div>
        </div>

        <div id="successScreen" style="display:none;position:fixed;inset:0;z-index:99999;padding:24px;background:
          radial-gradient(circle at top left, rgba(63,121,216,.10), transparent 32%),
          radial-gradient(circle at bottom right, rgba(63,121,216,.08), transparent 28%),
          linear-gradient(180deg,#f4f7fb 0%, #e9eef7 100%);
          align-items:center;justify-content:center;">
          <div style="width:min(760px,100%);background:rgba(255,255,255,.94);border:1px solid rgba(34,48,70,.10);border-radius:32px;box-shadow:0 20px 60px rgba(30,46,82,.12);padding:42px 28px 32px;text-align:center;position:relative;overflow:hidden;backdrop-filter:blur(10px);">
            <div style="position:relative;z-index:1;display:flex;flex-direction:column;align-items:center;justify-content:center;min-height:320px;gap:18px;">
              <div style="font-size:.84rem;letter-spacing:.14em;text-transform:uppercase;color:#5b6980;font-weight:700;">Segnalazione inviata</div>
              <div style="max-width:560px;margin:0 auto;font-family:Georgia,'Palatino Linotype','Book Antiqua',serif;font-style:italic;font-size:clamp(1.6rem,2.8vw,2.35rem);line-height:1.48;color:#1f2d42;letter-spacing:.01em;">
                Grazie per la segnalazione.<br>
                Il proprietario del veicolo è stato avvisato.
              </div>
              <div style="max-width:500px;margin:0 auto;color:#5b6980;font-size:1rem;line-height:1.55;">
                La tua collaborazione è stata utile e verrà gestita in modo riservato.
              </div>
              <button id="closeSuccessScreenBtn" type="button" style="margin-top:12px;border:none;border-radius:18px;min-height:52px;padding:0 26px;background:linear-gradient(180deg,#3f79d8 0%, #2f5fad 100%);color:#fff;font-weight:800;font-size:1rem;cursor:pointer;box-shadow:0 12px 28px rgba(63,121,216,.22);">Chiudi</button>
            </div>
          </div>
        </div>

        <div class="final-note">
          <strong>Usa questo servizio solo per segnalazioni reali.</strong><br>
          In caso di emergenza grave contatta subito i numeri di soccorso.
        </div>

        <div class="service-cta">
          <div class="service-cta-title">Vuoi essere avvisato subito<br><span style="white-space:nowrap;">anche per la tua vettura?</span></div>
          <a class="service-cta-link" href="https://adesivo-auto.onrender.com/scopri-servizio">Scopri il servizio</a>
        </div>
      </div>
    </div>
  </div>

  <script>
    const params = new URLSearchParams(window.location.search);
    const pathMatch = window.location.pathname.match(/\/contact\/u\/([A-Z0-9]+)/i);
    const publicId = ((params.get('public_id') || (pathMatch ? pathMatch[1] : ''))).trim().toUpperCase();

    const choices = [...document.querySelectorAll('.choice')];
    const errorBox = document.getElementById('errorBox');
    const successBox = document.getElementById('successBox');
    const sendBtn = document.getElementById('sendBtn');
    const reasonField = document.getElementById('reason');
    const msgField = document.getElementById('msg');
    const shareLocationField = document.getElementById('shareLocation');

    let contactData = null;
    let selectedReason = '';

    const presetMessages = {
      'Urtata accidentalmente': 'Volevo segnalarti che il veicolo potrebbe essere stato urtato accidentalmente.',
      'Intralcio al traffico': 'Volevo segnalarti che il veicolo si trova in una situazione di intralcio al traffico.',
      'Vetri abbassati / porta aperta': 'Volevo segnalarti che il veicolo risulta con vetri abbassati o una porta aperta.',
      'Bimbo o animale in auto': 'Volevo segnalarti che risulta presente un bimbo o un animale all’interno del veicolo.',
      'Furto / tentato furto': 'Volevo segnalarti un possibile furto o tentato furto relativo al veicolo.',
      'Varie': ''
    };

    choices.forEach(btn => {
      btn.addEventListener('click', () => {
        choices.forEach(x => x.classList.remove('selected'));
        btn.classList.add('selected');
        selectedReason = btn.dataset.value;
        reasonField.value = selectedReason;
        errorBox.style.display = 'none';
        successBox.style.display = 'none';

        msgField.value = presetMessages[selectedReason] || '';
      });
    });

    function showError(message){
      errorBox.textContent = message || 'Impossibile completare l’invio. Riprova più tardi.';
      errorBox.style.display = 'block';
      successBox.style.display = 'none';
      const successScreen = document.getElementById('successScreen');
      if (successScreen) successScreen.style.display = 'none';
      sendBtn.disabled = false;
      sendBtn.textContent = 'INVIA SEGNALAZIONE';
      errorBox.scrollIntoView({ behavior: 'smooth', block: 'center' });
    }

    function setVehicle(data){
      const vehicleName = [data.brand, data.vehicle_model].filter(Boolean).join(' ').trim() || data.vehicle_model || data.brand || 'Veicolo';
      document.getElementById('vehicleName').textContent = vehicleName;
      document.getElementById('vehiclePlate').textContent = `${data.plate || '---'}`;
    }

    function buildMessage(extraLocation = '') {
      const details = msgField.value.trim();

      let message = `SEGNALAZIONE VEICOLO\n`;
      message += `${contactData.plate || ''}\n`;
      if (selectedReason === 'Varie') {
        message += `Varie\n`;
      }
      if (details) message += `${details}\n`;
      if (extraLocation) message += extraLocation;
      return message.trim();
    }

    async function logInternalMessage(finalMessage, locationData = {}) {
      const res = await fetch('/api/log-contact-message', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          code: contactData.code,
          plate: contactData.plate,
          brand: contactData.brand,
          vehicle_model: contactData.vehicle_model,
          color: contactData.color,
          reason: selectedReason,
          message_text: finalMessage,
          sender_phone: null,
          location_shared: !!locationData.location_shared,
          latitude: locationData.latitude || null,
          longitude: locationData.longitude || null,
          maps_url: locationData.maps_url || null
        })
      });

      let data = {};
      try {
        data = await res.json();
      } catch (e) {
        data = {};
      }

      if (!res.ok || !data.success) {
        throw new Error((data && data.error) ? data.error : 'Impossibile completare l’invio. Riprova più tardi.');
      }

      return data;
    }

    function showSuccessScreen(){
      const successScreen = document.getElementById('successScreen');
      if (successScreen) successScreen.style.display = 'flex';
    }

    function closeSuccessScreen(){
      try {
        window.open('', '_self');
        window.close();
      } catch (e) {}
      document.body.innerHTML = '<div style="min-height:100vh;display:flex;align-items:center;justify-content:center;padding:24px;text-align:center;font-family:-apple-system,BlinkMacSystemFont,Segoe UI,Arial,sans-serif;color:#223046;background:#eef3fa;">Puoi chiudere questa pagina dal browser.</div>';
    }

    async function sendSignal(){
      errorBox.style.display = 'none';
      successBox.style.display = 'none';
      sendBtn.disabled = true;
      sendBtn.textContent = 'INVIO IN CORSO...';

      if (!selectedReason) {
        sendBtn.disabled = false;
        sendBtn.textContent = 'INVIA SEGNALAZIONE';
        errorBox.textContent = 'Seleziona il motivo della segnalazione per poter avvisare il proprietario.';
        errorBox.style.display = 'block';

        const choicesBox = document.getElementById('choices');
        if (choicesBox) {
          choicesBox.scrollIntoView({ behavior: 'smooth', block: 'center' });
          choicesBox.classList.remove('flash-attention');
          void choicesBox.offsetWidth;
          choicesBox.classList.add('flash-attention');
          setTimeout(() => {
            choicesBox.classList.remove('flash-attention');
          }, 1700);
        }

        setTimeout(() => {
          const firstChoice = document.querySelector('.choice');
          if (firstChoice) {
            firstChoice.focus?.();
          }
        }, 350);

        return;
      }

      const baseMessage = buildMessage();

      try {
        if (!shareLocationField.checked) {
          await logInternalMessage(baseMessage);
          showSuccessScreen();
          return;
        }

        if (!navigator.geolocation) {
          await logInternalMessage(baseMessage);
          showSuccessScreen();
          return;
        }

        navigator.geolocation.getCurrentPosition(
          async (position) => {
            try {
              const lat = position.coords.latitude.toFixed(6);
              const lng = position.coords.longitude.toFixed(6);
              const mapsUrl = `https://maps.google.com/?q=${lat},${lng}`;
              const extraLocation = `Posizione: ${lat}, ${lng}\nMappa: ${mapsUrl}\n`;
              const finalMessage = buildMessage(extraLocation);

              await logInternalMessage(finalMessage, {
                location_shared: true,
                latitude: lat,
                longitude: lng,
                maps_url: mapsUrl
              });

              showSuccessScreen();
            } catch (err) {
              showError(err && err.message ? err.message : 'Impossibile completare l’invio. Riprova più tardi.');
            }
          },
          async () => {
            try {
              await logInternalMessage(baseMessage);
              showSuccessScreen();
            } catch (err) {
              showError(err && err.message ? err.message : 'Impossibile completare l’invio. Riprova più tardi.');
            }
          },
          { enableHighAccuracy: true, timeout: 10000, maximumAge: 0 }
        );
      } catch (err) {
        showError(err && err.message ? err.message : 'Impossibile completare l’invio. Riprova più tardi.');
      }
    }
    sendBtn.addEventListener('click', sendSignal);
    document.getElementById('closeSuccessScreenBtn').addEventListener('click', closeSuccessScreen);

    async function init() {
      if (!publicId) return;
      const response = await fetch(`/api/public-contact/${encodeURIComponent(publicId)}`);
      const payload = await response.json();
      if (!response.ok || !payload.success) return;
      contactData = payload.data;
      setVehicle(contactData);
    }

    init();
  </script>

</body>
</html>
