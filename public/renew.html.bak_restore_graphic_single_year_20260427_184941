<!DOCTYPE html>
<html lang="it">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Piani e rinnovo • Contatto Veicolo</title>
  <meta name="description" content="Rinnova il servizio Contatto Veicolo per continuare a ricevere segnalazioni riservate e notifiche dal tuo QR." />
  <style>
    :root{
      --bg1:#07111c;
      --bg2:#0b1930;
      --card:rgba(255,255,255,.075);
      --card2:rgba(255,255,255,.045);
      --line:rgba(255,255,255,.13);
      --text:#eff5ff;
      --muted:#b8c7dc;
      --soft:#dfe9f7;
      --accent:#f0b90b;
      --accent2:#ffd75a;
      --blue:#3f79d8;
      --green:#35c279;
      --danger:#ff6678;
      --shadow:0 26px 70px rgba(0,0,0,.34);
      --radius:30px;
      --max:980px;
    }

    *{box-sizing:border-box}

    body{
      margin:0;
      min-height:100vh;
      font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",Roboto,Helvetica,Arial,sans-serif;
      color:var(--text);
      background:
        radial-gradient(900px 460px at 50% -12%, rgba(63,121,216,.46), transparent 60%),
        radial-gradient(520px 380px at 12% 8%, rgba(240,185,11,.16), transparent 60%),
        linear-gradient(180deg,var(--bg2) 0%, var(--bg1) 48%, #050c15 100%);
      line-height:1.45;
    }

    a{color:inherit;text-decoration:none}
    .wrap{width:min(var(--max),100%);margin:0 auto;padding:34px 18px 56px}
    .top{text-align:center;padding:28px 0 18px}
    .eyebrow{
      display:inline-flex;
      align-items:center;
      justify-content:center;
      padding:9px 14px;
      border:1px solid var(--line);
      border-radius:999px;
      color:#dce8fb;
      background:rgba(255,255,255,.055);
      font-size:.84rem;
      font-weight:800;
      letter-spacing:.04em;
      text-transform:uppercase;
    }

    h1{
      margin:18px auto 12px;
      max-width:820px;
      font-size:clamp(2.2rem,6vw,4.4rem);
      line-height:.98;
      letter-spacing:-.055em;
    }

    .lead{
      max-width:730px;
      margin:0 auto;
      color:var(--muted);
      font-size:clamp(1.02rem,2vw,1.22rem);
    }

    .vehicle-card{
      margin:28px auto 20px;
      padding:18px;
      border:1px solid var(--line);
      border-radius:24px;
      background:linear-gradient(180deg,var(--card),var(--card2));
      box-shadow:var(--shadow);
      display:none;
    }

    .vehicle-grid{
      display:grid;
      grid-template-columns:1.2fr .8fr;
      gap:12px;
      align-items:center;
    }

    .mini-label{
      color:var(--muted);
      text-transform:uppercase;
      letter-spacing:.08em;
      font-size:.72rem;
      font-weight:900;
      margin-bottom:4px;
    }

    .vehicle-main{
      font-size:1.28rem;
      font-weight:950;
      letter-spacing:-.02em;
    }

    .vehicle-sub{
      color:var(--muted);
      font-size:.96rem;
      margin-top:3px;
    }

    .status-pill{
      justify-self:end;
      display:inline-flex;
      align-items:center;
      justify-content:center;
      min-height:38px;
      padding:0 13px;
      border-radius:999px;
      border:1px solid rgba(255,255,255,.14);
      background:rgba(255,255,255,.06);
      color:#eaf2ff;
      font-weight:900;
      font-size:.86rem;
      text-align:center;
    }

    .status-pill.ok{background:rgba(53,194,121,.13);border-color:rgba(53,194,121,.32);color:#bff7d8}
    .status-pill.warn{background:rgba(240,185,11,.13);border-color:rgba(240,185,11,.35);color:#ffe9a4}
    .status-pill.expired{background:rgba(255,102,120,.13);border-color:rgba(255,102,120,.35);color:#ffd0d6}

    .panel{
      margin-top:22px;
      border:1px solid var(--line);
      border-radius:var(--radius);
      background:linear-gradient(180deg,rgba(255,255,255,.08),rgba(255,255,255,.04));
      box-shadow:var(--shadow);
      overflow:hidden;
    }

    .plan{
      display:grid;
      grid-template-columns:1fr .88fr;
      gap:0;
    }

    .plan-copy{padding:30px}
    .plan-side{
      padding:30px;
      border-left:1px solid var(--line);
      background:
        radial-gradient(420px 260px at 50% 0%, rgba(240,185,11,.20), transparent 70%),
        rgba(255,255,255,.035);
      display:flex;
      flex-direction:column;
      justify-content:center;
    }

    .plan-title{
      display:flex;
      align-items:center;
      gap:10px;
      margin-bottom:10px;
    }

    .badge{
      min-width:42px;
      width:42px;
      height:42px;
      border-radius:15px;
      display:inline-flex;
      align-items:center;
      justify-content:center;
      background:linear-gradient(180deg,var(--accent2),var(--accent));
      color:#111;
      font-weight:1000;
      box-shadow:0 16px 34px rgba(240,185,11,.18);
    }

    h2{
      margin:0;
      font-size:clamp(1.6rem,3vw,2.35rem);
      line-height:1.03;
      letter-spacing:-.04em;
    }

    .plan-copy p{
      color:var(--muted);
      margin:12px 0 0;
      font-size:1.03rem;
    }

    .checks{
      display:grid;
      gap:12px;
      margin-top:22px;
    }

    .check{
      display:flex;
      gap:11px;
      color:#eaf2ff;
      align-items:flex-start;
      font-weight:700;
    }

    .check span:first-child{
      color:#111;
      background:linear-gradient(180deg,var(--accent2),var(--accent));
      width:22px;
      height:22px;
      min-width:22px;
      border-radius:999px;
      display:inline-flex;
      align-items:center;
      justify-content:center;
      font-size:.84rem;
      font-weight:1000;
      margin-top:1px;
    }

    .price-box{
      border:1px solid rgba(240,185,11,.34);
      background:linear-gradient(180deg,rgba(240,185,11,.13),rgba(240,185,11,.055));
      border-radius:26px;
      padding:24px;
      text-align:center;
    }

    .price-label{
      color:#fff4c9;
      font-size:.8rem;
      letter-spacing:.1em;
      text-transform:uppercase;
      font-weight:950;
    }

    .price{
      margin:8px 0 2px;
      color:#fff;
      font-size:clamp(3rem,7vw,4.4rem);
      letter-spacing:-.08em;
      line-height:.92;
      font-weight:1000;
    }

    .price small{
      font-size:1.4rem;
      letter-spacing:-.04em;
      color:#fff0b0;
    }

    .per{
      color:#f7df91;
      font-weight:850;
    }

    .cta{
      margin-top:18px;
      width:100%;
      min-height:58px;
      border:0;
      border-radius:18px;
      cursor:pointer;
      display:inline-flex;
      align-items:center;
      justify-content:center;
      text-align:center;
      background:linear-gradient(180deg,var(--accent2),var(--accent));
      color:#111;
      font-size:1.04rem;
      font-weight:1000;
      box-shadow:0 20px 44px rgba(240,185,11,.22);
    }

    .cta.disabled{
      cursor:not-allowed;
      opacity:.74;
      filter:grayscale(.25);
    }

    .note{
      margin-top:12px;
      color:var(--muted);
      font-size:.92rem;
      text-align:center;
    }

    .free-box{
      display:none;
      margin-top:18px;
      padding:18px;
      border-radius:22px;
      border:1px solid rgba(53,194,121,.30);
      background:rgba(53,194,121,.10);
      color:#c9f8dd;
      font-weight:780;
    }

    .info-grid{
      display:grid;
      grid-template-columns:repeat(3,1fr);
      gap:14px;
      margin-top:18px;
    }

    .info{
      padding:16px;
      border:1px solid var(--line);
      border-radius:20px;
      background:rgba(255,255,255,.045);
    }

    .info strong{
      display:block;
      color:#fff;
      font-size:1rem;
      margin-bottom:5px;
    }

    .info span{
      color:var(--muted);
      font-size:.94rem;
    }

    .footer{
      padding:26px 0 0;
      text-align:center;
      color:#91a3bc;
      font-size:.92rem;
    }

    .back-link{
      margin-top:16px;
      display:inline-flex;
      color:#dbe7f8;
      font-weight:850;
      opacity:.85;
    }

    @media(max-width:820px){
      .plan{grid-template-columns:1fr}
      .plan-side{border-left:0;border-top:1px solid var(--line)}
      .vehicle-grid{grid-template-columns:1fr}
      .status-pill{justify-self:start}
      .info-grid{grid-template-columns:1fr}
      .plan-copy,.plan-side{padding:22px}
    }
  </style>
</head>
<body>
  <div class="wrap">
    <header class="top">
      <div class="eyebrow">Contatto Veicolo • Piani e rinnovo</div>
      <h1>Continua a ricevere gli avvisi del tuo veicolo.</h1>
      <p class="lead">
        Dopo la prova gratuita di 30 giorni, puoi mantenere attivo il tuo QR con un unico rinnovo annuale semplice, chiaro e senza funzioni inutili.
      </p>
    </header>

    <section id="vehicleCard" class="vehicle-card">
      <div class="vehicle-grid">
        <div>
          <div class="mini-label">Servizio associato a</div>
          <div class="vehicle-main" id="vehicleName">Contatto Veicolo</div>
          <div class="vehicle-sub" id="vehiclePlate">Targa non specificata</div>
        </div>
        <div class="status-pill" id="serviceStatus">Verifica stato...</div>
      </div>
    </section>

    <main class="panel">
      <div class="plan">
        <div class="plan-copy">
          <div class="plan-title">
            <div class="badge">1</div>
            <h2>Rinnovo annuale</h2>
          </div>

          <p>
            Mantieni attiva la pagina QR del tuo veicolo e continua a ricevere segnalazioni riservate dai passanti quando qualcuno nota un problema reale.
          </p>

          <div class="checks">
            <div class="check"><span>✓</span><div>QR personale del veicolo sempre collegato alla tua pagina di segnalazione.</div></div>
            <div class="check"><span>✓</span><div>Segnalazioni riservate: chi avvisa non deve telefonare e non vede il tuo numero.</div></div>
            <div class="check"><span>✓</span><div>Notifiche sul dispositivo registrato quando arriva una nuova segnalazione.</div></div>
            <div class="check"><span>✓</span><div>Cartello stampabile collegato al tuo QR, utilizzabile quando parcheggi.</div></div>
          </div>

          <div class="free-box" id="freeBox">
            Questo servizio risulta offerto gratuitamente in modo permanente. Non è necessario acquistare un rinnovo.
          </div>
        </div>

        <div class="plan-side">
          <div class="price-box">
            <div class="price-label">Unico piano disponibile</div>
            <div class="price">9,90<small>€</small></div>
            <div class="per">per 1 anno</div>

            <a id="payBtn" class="cta disabled" href="#" aria-disabled="true">
              Rinnova per 1 anno
            </a>

            <div class="note" id="paymentNote">
              Link pagamento in aggiornamento. Crea il Payment Link Stripe annuale da 9,90 € e inseriscilo qui.
            </div>
          </div>
        </div>
      </div>
    </main>

    <section class="info-grid">
      <div class="info">
        <strong>Un solo prezzo</strong>
        <span>Niente pacchetti multipli: un rinnovo annuale semplice da 9,90 €.</span>
      </div>
      <div class="info">
        <strong>Niente carta per la prova</strong>
        <span>La prova gratuita resta senza vincolo e senza inserimento carta.</span>
      </div>
      <div class="info">
        <strong>Solo servizio QR</strong>
        <span>Nessuna promessa su promemoria scadenze o servizi aggiuntivi futuri.</span>
      </div>
    </section>

    <footer class="footer">
      <div>Contatto Veicolo • Rinnovo annuale</div>
      <a class="back-link" href="/scopri-servizio">Torna alla presentazione del servizio</a>
    </footer>
  </div>

  <script>
    /*
      IMPORTANTE:
      Inserisci qui il Payment Link Stripe reale da 9,90 €/anno.
      Finché rimane vuoto, il pulsante resta disattivato per evitare pagamenti al prezzo vecchio.
    */
    const ANNUAL_PAYMENT_LINK_990 = "";

    const params = new URLSearchParams(window.location.search);
    const pathMatch = window.location.pathname.match(/\/renew\/u\/([A-Z0-9]+)/i);
    const publicId = ((params.get('public_id') || (pathMatch ? pathMatch[1] : '')) || '').trim().toUpperCase();

    const vehicleCard = document.getElementById('vehicleCard');
    const vehicleName = document.getElementById('vehicleName');
    const vehiclePlate = document.getElementById('vehiclePlate');
    const serviceStatus = document.getElementById('serviceStatus');
    const payBtn = document.getElementById('payBtn');
    const paymentNote = document.getElementById('paymentNote');
    const freeBox = document.getElementById('freeBox');

    function fmtDate(v){
      if(!v) return '';
      try { return new Date(v).toLocaleDateString('it-IT'); } catch(e){ return ''; }
    }

    function setPaymentLink(data){
      if (!ANNUAL_PAYMENT_LINK_990) {
        payBtn.classList.add('disabled');
        payBtn.setAttribute('aria-disabled', 'true');
        payBtn.href = '#';
        paymentNote.textContent = 'Link pagamento in aggiornamento. Crea il Payment Link Stripe annuale da 9,90 € e inseriscilo qui.';
        return;
      }

      let url = ANNUAL_PAYMENT_LINK_990;

      try {
        const u = new URL(url);
        if (data && data.public_id) {
          u.searchParams.set('client_reference_id', data.public_id);
        } else if (publicId) {
          u.searchParams.set('client_reference_id', publicId);
        }
        url = u.toString();
      } catch(e) {}

      payBtn.href = url;
      payBtn.classList.remove('disabled');
      payBtn.removeAttribute('aria-disabled');
      paymentNote.textContent = 'Pagamento sicuro. Il rinnovo mantiene attivo il servizio per 1 anno.';
    }

    payBtn.addEventListener('click', function(e){
      if (payBtn.classList.contains('disabled')) {
        e.preventDefault();
        alert('Il link di pagamento annuale da 9,90 € non è ancora stato configurato.');
      }
    });

    async function loadVehicle(){
      if (!publicId) {
        setPaymentLink(null);
        return;
      }

      vehicleCard.style.display = 'block';

      try {
        const res = await fetch('/api/public-renew/' + encodeURIComponent(publicId));
        const data = await res.json();

        if (!res.ok || !data.success) {
          serviceStatus.textContent = 'Dati non disponibili';
          serviceStatus.className = 'status-pill warn';
          setPaymentLink({ public_id: publicId });
          return;
        }

        const row = data.record || data.vehicle || data;

        const title = [row.brand, row.vehicle_model].filter(Boolean).join(' ').trim() || 'Contatto Veicolo';
        vehicleName.textContent = title;
        vehiclePlate.textContent = row.plate ? ('Targa ' + row.plate) : 'Targa non specificata';

        const isAlwaysGift = row.plan_type === 'always' && !row.expires_at && row.offered_by && row.offered_by !== 'Prova gratuita';

        if (isAlwaysGift) {
          serviceStatus.textContent = 'Omaggio permanente';
          serviceStatus.className = 'status-pill ok';
          freeBox.style.display = 'block';
          payBtn.classList.add('disabled');
          payBtn.setAttribute('aria-disabled','true');
          payBtn.href = '#';
          paymentNote.textContent = 'Questo servizio risulta offerto gratuitamente in modo permanente.';
          return;
        }

        if (row.expires_at) {
          const exp = new Date(row.expires_at);
          const now = new Date();
          const diffDays = Math.ceil((exp - now) / (1000 * 60 * 60 * 24));

          if (diffDays < 0) {
            serviceStatus.textContent = 'Scaduto';
            serviceStatus.className = 'status-pill expired';
          } else if (diffDays <= 15) {
            serviceStatus.textContent = 'Scade il ' + fmtDate(row.expires_at);
            serviceStatus.className = 'status-pill warn';
          } else {
            serviceStatus.textContent = 'Attivo fino al ' + fmtDate(row.expires_at);
            serviceStatus.className = 'status-pill ok';
          }
        } else {
          serviceStatus.textContent = 'Rinnovo disponibile';
          serviceStatus.className = 'status-pill warn';
        }

        setPaymentLink(row);
      } catch(e) {
        serviceStatus.textContent = 'Errore verifica stato';
        serviceStatus.className = 'status-pill warn';
        setPaymentLink({ public_id: publicId });
      }
    }

    loadVehicle();
  </script>
</body>
</html>
